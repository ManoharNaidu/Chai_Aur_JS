// switch
switch (key) {
  case value:
    break;
  default:
    break;
  // if break is not included in every case then it will execute all the cases after the matching case except the default case
}
